---
name: tm-meeting-poster-generator
description: 头马俱乐部例会宣传海报生成器。根据例会信息（俱乐部名/期数/主题/时间/地址/亮点等），生成符合头马品牌规范（5O法则）的竖版宣传海报。支持常规例会和大咖工作坊两种模式，大咖人像可选。默认3:4竖版，可自定义9:16/1:1。仅支持中文。
---

# 头马例会海报生成器

## 适用场景

- 头马俱乐部常规例会宣发海报
- 含大咖嘉宾/工作坊的特别例会海报
- 可复用于不同俱乐部、不同期数

**不适用**：非头马品牌海报、横向Banner、H5长页、视频封面。

## 环境依赖

本 Skill 的后期合成（贴 logo、底部排版）使用 Python PIL 脚本，需要以下环境：

| 依赖 | 说明 |
|------|------|
| Python 3.7+ | 运行合成脚本 |
| Pillow (PIL) | 图像处理库，`pip install Pillow` |
| pyzbar（可选） | 二维码自动解码检查，`pip install pyzbar`，未安装时跳过自动检查 |

**豆包工作环境**：默认已安装 Python 和 Pillow，可直接运行脚本。

**分发到其他环境**：
- 如果对方环境有 Python + Pillow，脚本可直接运行
- 如果没有 Python 环境，可使用**替代方案**：用 AI 直接生成完整海报（包含 logo 和二维码），但需注意：
  - AI 生成的 logo 可能变形，需手动检查
  - AI 生成的二维码可能不可扫，建议后期手动贴二维码
  - 底部文字排版可能不精确，需在 prompt 中详细描述

**安装依赖命令**：
```bash
pip install Pillow
pip install pyzbar  # 可选，用于二维码自动检查
```

## 输入参数清单

| 参数 | 必填 | 说明 |
|------|------|------|
| 俱乐部名称 | 是 | 如"Talktime头马国际演讲会" |
| 期数 | 是 | 如"第444期例会" |
| 会议主题 | 是 | 如"听见心底的声音"（驱动视觉元素设计） |
| 今日一词 | 否 | 如"回响" |
| 时间 | 是 | 日期+星期+时段，如"2026年6月28日 周日 14:30-17:30" |
| 地址 | 是 | 详细地址 |
| 亮点列表 | 否 | 如"半年愿望信笺拆封、大咖工作坊、会员颁奖" |
| 海报比例 | 否 | 默认3:4，可选9:16/1:1 |
| 是否大咖工作坊 | 是 | 布尔值，决定走哪条流程 |
| 工作坊题目 | 条件必填 | 大咖模式下必填 |
| 讲师姓名+身份 | 条件必填 | 大咖模式下必填 |
| 是否提供大咖人像 | 条件必填 | 大咖模式下询问，用户可选提供或不提供 |
| 大咖人像素材路径 | 条件可选 | 用户选择提供时必填；不提供则跳过 |
| 二维码路径 | 是 | VPM微信二维码，用户自行提供 |

**内置素材**：头马官方彩色logo已内置，位于 `assets/logo/toastmasters-logo-color.png`，无需用户提供。

## 核心工作流程（6步）

### Step 1: 需求收集与确认

收集所有必填参数，询问以下可选项：
- 是否有大咖工作坊？→ 是 → 询问是否提供大咖人像？（用户可选）
- 海报比例？（默认3:4，可改9:16/1:1）

确认所有信息无误后进入下一步。

### Step 2: 品牌规范预检

读取 `references/brand-guidelines.md`，确认：
- 品牌色：主色栗红 #772432 和 忠诚蓝 #004165（两者都是主色，不分主次），强调色快乐黄 #F2DF74
- 字体：思源黑体 Noto Sans SC（免费商用），路径 `C:\Windows\Fonts\Noto Sans SC (TrueType).otf`
- Logo规范：彩色原版，最小72px，周边留白=字标高度
- 渐变只允许同色渐变

### Step 3: 视觉元素 + 版面结构方案设计与确认

**第一次生图前必须完成双确认，禁止跳过。**

#### 3a. 视觉元素方案设计

根据当期会议主题，发散设计 2-3 套视觉元素方案（纯文字描述），每套包含：
- 主视觉意象
- 装饰元素
- 光影氛围
- 色彩倾向（在品牌色框架内）
- 与主题关联说明

**⚠️ 不预设固定视觉元素清单，完全由主题驱动。** 禁止每期都用相同的麦克风/声波/信笺等元素。

#### 3b. 版面结构方案输出

**必须使用 ASCII 示意图格式输出**，直观展示整张海报的版面结构，让用户一眼就能理解各区域的位置和占比。禁止使用纯像素坐标列表（如"y: 0-180"）作为主要呈现方式。

输出内容包含：
- 画幅比例和尺寸
- ASCII 示意图（标注各区域名称和大致占比）
- 各区域简要说明（内容、对齐方式、颜色）
- 安全边距说明

详细模板见 `references/layout-spec.md` 中的"版面结构方案输出模板"。

#### 跟用户确认

视觉元素方案 + 版面结构方案都确认后，才进入 Step 4 第一次生图。

### Step 4: AI生成背景主视觉

根据模式选择工具：

| 模式 | 工具 | 说明 |
|------|------|------|
| 常规例会 | `image_gen` 文生图 | 将确认后的视觉元素方案填入Prompt |
| 大咖工作坊（提供人像） | `image_edit` 图生图 | [img0]=无人像背景，[img1]=大咖人像，人像作为参考融入背景 |
| 大咖工作坊（不提供人像） | `image_gen` 文生图 | 用抽象/象征性视觉元素代表大咖 |

Prompt模板见 `references/prompt-templates.md`。

**生成后检查**：主标题文字是否正确、视觉元素是否符合方案、人物是否遮挡文字、整体协调性。

### Step 5: PIL精确合成

**推荐方式（有 Python 环境）**：运行 `scripts/compose_poster.py`，完成：
1. 贴头马logo（左上角，规范留白）
2. 底部左右分栏排版：
   - 左侧：时间（快乐黄粗体）+ 地址（白色常规），左对齐
   - 右侧：微信二维码（白色圆角底衬），右对齐
   - 二维码左侧竖排标签："扫码加入我们"
3. 必要时文字精确重写（确保居中/对齐，像素级控制）

**⚠️ 禁止用AI生成logo和二维码**（会变形/不可扫），一律用PIL精确贴图。

**替代方式（无 Python 环境）**：
- 用 AI 直接生成完整海报（在 prompt 中详细描述 logo 位置、底部排版、二维码位置）
- 生成后必须手动检查：logo 是否变形、二维码是否可扫、底部文字是否清晰
- 如果二维码不可扫，建议用图片编辑工具手动贴二维码原图
- 注意：AI 生成的完整海报精度不如 PIL 合成，logo 和二维码可能有瑕疵

### Step 6: 验证与交付

1. 运行 `scripts/verify_brand.py` 进行品牌合规检查
2. 人工检查：文字准确性、整体协调性、二维码可扫性
3. 交付最终海报PNG

## 品牌规范摘要

详细规范见 `references/brand-guidelines.md`。

| 项目 | 规范 |
|------|------|
| 主色 | 栗红 #772432 RGB(119,36,50) 和 忠诚蓝 #004165 RGB(0,65,101)，**两者都是主色，不分主次**，每期可选择其一或搭配使用 |
| 强调色 | 快乐黄 #F2DF74 RGB(242,223,116)（仅点缀，不宜大面积） |
| 中性色 | 白色 #FFFFFF、黑色 #000000、冷灰 #A9B2B1 |
| 渐变 | 只允许同色渐变，禁止跨色渐变 |
| 字体 | 思源黑体 Noto Sans SC（免费商用），中文推荐黑体/宋体/等线 |
| Logo | 彩色原版，最小72px（2cm），周边留白=字标高度，禁止变形/特效/放在非标准色背景 |

## 输出产物

- 最终海报 PNG（默认1536×2048，3:4）
- 可选：品牌合规检查报告

## 文件结构

```
tm-meeting-poster-generator/
├── SKILL.md                          # 本文件
├── references/
│   ├── brand-guidelines.md           # 头马品牌规范（5O法则梳理版）
│   ├── layout-spec.md                # 海报版式规范
│   ├── prompt-templates.md           # AI生图Prompt模板库
│   └── troubleshooting.md            # 常见问题与避坑指南
├── scripts/
│   ├── compose_poster.py             # PIL合成脚本
│   └── verify_brand.py               # 品牌合规检查脚本
├── assets/
│   ├── logo/
│   │   └── toastmasters-logo-color.png  # 内置头马官方彩色logo
│   └── README.md                     # 素材说明
└── examples/
    ├── talktime-444/                 # 第一期示例（常规例会）
    │   ├── final.png
    │   └── info.md
    └── mingyuan-388/                 # 第二期示例（大咖工作坊）
        ├── final.png
        └── info.md
```

## 注意事项

1. **视觉元素必须主题驱动**：禁止每期使用相同的固定元素，每次根据主题设计新方案
2. **第一次生图前必须双确认**：视觉元素方案 + 版面结构方案都要跟用户确认
3. **大咖人像可选**：有工作坊也可以不提供人像，用户自行决定
4. **PIL合成不可省略**：logo、二维码、底部文字必须用PIL精确合成，不能依赖AI生成
5. **每次修改后做整体检查**：不能只改用户指出的单点，要重新判断整体协调性